
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Créer une Annonce - LuxStay</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/feather-icons/4.29.0/feather.min.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-rose-50 to-orange-50 min-h-screen">
    <!-- Navigation -->
    <nav class="fixed w-full z-50 glass-effect border-b border-white border-opacity-20">
        <!-- Votre code de navigation ici -->
    </nav>

    <!-- Main Content -->
    <div class="pt-20 container mx-auto px-4">
        <h1 class="text-4xl font-bold text-gray-800 mb-8">Créer une Annonce</h1>

        <!-- Formulaire de création d'annonce -->
        <div class="glass-effect rounded-2xl shadow-2xl p-6">
            <form class="space-y-6">
                <!-- Titre -->
                <div>
                    <label for="title" class="block text-gray-700 font-semibold mb-2">Titre de l'annonce</label>
                    <input type="text" id="title" placeholder="Titre" class="w-full px-4 py-3 rounded-xl bg-white bg-opacity-50 focus:ring-2 focus:ring-rose-400 outline-none">
                </div>

                <!-- Description -->
                <div>
                    <label for="description" class="block text-gray-700 font-semibold mb-2">Description</label>
                    <textarea id="description" placeholder="Description" rows="4" class="w-full px-4 py-3 rounded-xl bg-white bg-opacity-50 focus:ring-2 focus:ring-rose-400 outline-none"></textarea>
                </div>

                <!-- Localisation -->
                <div>
                    <label for="location" class="block text-gray-700 font-semibold mb-2">Localisation</label>
                    <input type="text" id="location" placeholder="Localisation" class="w-full px-4 py-3 rounded-xl bg-white bg-opacity-50 focus:ring-2 focus:ring-rose-400 outline-none">
                </div>

                <!-- Prix -->
                <div>
                    <label for="price" class="block text-gray-700 font-semibold mb-2">Prix par nuit</label>
                    <input type="number" id="price" placeholder="Prix" class="w-full px-4 py-3 rounded-xl bg-white bg-opacity-50 focus:ring-2 focus:ring-rose-400 outline-none">
                </div>

                <!-- Image -->
                <div>
                    <label for="image" class="block text-gray-700 font-semibold mb-2">Image</label>
                    <input type="file" id="image" class="w-full px-4 py-3 rounded-xl bg-white bg-opacity-50 focus:ring-2 focus:ring-rose-400 outline-none">
                </div>

                <!-- Bouton de soumission -->
                <button type="submit" class="bg-gradient-to-r from-rose-500 to-orange-500 text-white px-6 py-3 rounded-xl hover:opacity-90 transition-opacity">
                    Créer l'annonce
                </button>
            </form>
        </div>
    </div>

    <script>
        feather.replace();
    </script>
</body>
</html>