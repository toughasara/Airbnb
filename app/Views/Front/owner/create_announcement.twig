<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Créer une Annonce - LuxStay</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/feather-icons/4.29.0/feather.min.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .glass-effect {
            background: rgba(255, 255, 255, 0.7);
            backdrop-filter: blur(10px);
        }
        .gradient-bg {
            background: linear-gradient(135deg, #FF6B6B, #FFB88C);
        }
    </style>
</head>
<body class="bg-gradient-to-br from-rose-50 to-orange-50 min-h-screen">
    <!-- Header -->
    <nav class="fixed w-full z-50 glass-effect border-b border-white border-opacity-20">
        <div class="container mx-auto px-4">
            <div class="flex justify-between items-center h-20">
                <!-- Logo -->
                <div class="flex items-center space-x-2">
                    <i data-feather="home" class="w-8 h-8 text-rose-500"></i>
                    <span class="text-2xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-rose-500 to-orange-500">
                        LuxStay
                    </span>
                </div>

                <!-- Navigation Links -->
                <div class="hidden md:flex items-center space-x-8">
                    <a href="/dashboardowner" class="nav-link text-gray-700 hover:text-rose-500 transition-colors">Tableau de Bord</a>
                    <a href="/addannounce" class="nav-link text-gray-700 hover:text-rose-500 transition-colors">Créer une Annonce</a>
                    <a href="/updateannounce" class="nav-link text-gray-700 hover:text-rose-500 transition-colors">Mettre à jour</a>
                </div>

                <!-- Auth Buttons -->
                <div class="flex items-center space-x-4">
                    <a href="/logout" class="px-4 py-2 text-rose-500 hover:text-rose-600 transition-colors">
                        Déconnexion
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="pt-20 container mx-auto px-4">
        <h1 class="text-4xl font-bold text-gray-800 mb-8">Créer une Annonce</h1>
        <!-- Formulaire de création d'annonce -->
        <div class="glass-effect rounded-2xl shadow-2xl p-6">
            <form method="post" action="/addannounce" class="space-y-6" >
                <!-- Titre -->
                <div>
                    <label for="title" class="block text-gray-700 font-semibold mb-2">Titre de l'annonce</label>
                    <input type="text" id="title" name="title" placeholder="Titre" class="w-full px-4 py-3 rounded-xl bg-white bg-opacity-50 focus:ring-2 focus:ring-rose-400 outline-none">
                </div>

                <!-- Description -->
                <div>
                    <label for="description" class="block text-gray-700 font-semibold mb-2">Description</label>
                    <textarea id="description" name="description" placeholder="Description" rows="4" class="w-full px-4 py-3 rounded-xl bg-white bg-opacity-50 focus:ring-2 focus:ring-rose-400 outline-none"></textarea>
                </div>

                <!-- Catégorie -->
                <div>
                    <label for="category" class="block text-gray-700 font-semibold mb-2">Catégorie</label>
                    <select id="category" name="category" class="w-full px-4 py-3 rounded-xl bg-white bg-opacity-50 focus:ring-2 focus:ring-rose-400 outline-none">
                        <option value="appartement">Appartement</option>
                        <option value="maison">Maison</option>
                        <option value="villa">Villa</option>
                        <option value="chalet">Chalet</option>
                    </select>
                </div>

                <!-- Prix par nuit -->
                <div>
                    <label for="price_per_night" class="block text-gray-700 font-semibold mb-2">Prix par nuit</label>
                    <input type="number" id="price_per_night" name="price_per_night" placeholder="Prix par nuit" class="w-full px-4 py-3 rounded-xl bg-white bg-opacity-50 focus:ring-2 focus:ring-rose-400 outline-none">
                </div>

                <!-- Nombre maximum d'invités -->
                <div>
                    <label for="max_guests" class="block text-gray-700 font-semibold mb-2">Nombre maximum d'invités</label>
                    <input type="number" id="max_guests" name="max_guests" placeholder="Nombre maximum d'invités" class="w-full px-4 py-3 rounded-xl bg-white bg-opacity-50 focus:ring-2 focus:ring-rose-400 outline-none">
                </div>

                <!-- Équipements -->
                <div>
                    <label for="amenities" class="block text-gray-700 font-semibold mb-2">Équipements</label>
                    <input type="text" id="amenities" name="amenities" placeholder="Équipements (séparés par des virgules)" class="w-full px-4 py-3 rounded-xl bg-white bg-opacity-50 focus:ring-2 focus:ring-rose-400 outline-none">
                </div>

                <!-- Photos -->
                <div>
                    <label for="photos" class="block text-gray-700 font-semibold mb-2">Photos</label>
                    <input type="text" id="photos" name="photos" multiple class="w-full px-4 py-3 rounded-xl bg-white bg-opacity-50 focus:ring-2 focus:ring-rose-400 outline-none">
                </div>

                <!-- Adresse -->
                <div>
                    <label for="address" class="block text-gray-700 font-semibold mb-2">Adresse</label>
                    <input type="text" id="address" name="address" placeholder="Adresse" class="w-full px-4 py-3 rounded-xl bg-white bg-opacity-50 focus:ring-2 focus:ring-rose-400 outline-none">
                </div>

                <!-- Ville -->
                <div>
                    <label for="city" class="block text-gray-700 font-semibold mb-2">Ville</label>
                    <input type="text" id="city" name="city" placeholder="Ville" class="w-full px-4 py-3 rounded-xl bg-white bg-opacity-50 focus:ring-2 focus:ring-rose-400 outline-none">
                </div>

                <!-- Pays -->
                <div>
                    <label for="country" class="block text-gray-700 font-semibold mb-2">Pays</label>
                    <input type="text" id="country" name="country" placeholder="Pays" class="w-full px-4 py-3 rounded-xl bg-white bg-opacity-50 focus:ring-2 focus:ring-rose-400 outline-none">
                </div>

                <!-- Bouton de soumission -->
                <button name="addAnnounce" type="submit" class="bg-gradient-to-r from-rose-500 to-orange-500 text-white px-6 py-3 rounded-xl hover:opacity-90 transition-opacity">
                    Créer l'annonce
                </button>
            </form>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-gradient-to-br from-rose-50 to-orange-50 border-t border-white border-opacity-20 mt-20">
        <div class="container mx-auto px-4 py-12">
            <div class="text-center text-gray-600">
                <p>&copy; 2023 LuxStay. Tous droits réservés.</p>
            </div>
        </div>
    </footer>

    <script>
        feather.replace();
    </script>
</body>
</html>