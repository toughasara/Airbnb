<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LuxStay - Inscription</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/feather-icons/4.29.0/feather.min.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .glass-effect {
            background: rgba(255, 255, 255, 0.7);
            backdrop-filter: blur(10px);
        }
        .gradient-border {
            border: 2px solid;
            border-image: linear-gradient(45deg, #FF6B6B, #FFB88C) 1;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-rose-50 to-orange-50 min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-md w-full space-y-8 glass-effect p-8 rounded-2xl shadow-2xl">
        <!-- Header -->
        <div class="text-center">
            <div class="flex items-center justify-center space-x-2 mb-6">
                <i data-feather="home" class="w-8 h-8 text-rose-500"></i>
                <span class="text-2xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-rose-500 to-orange-500">
                    LuxStay
                </span>
            </div>
            <h2 class="text-3xl font-bold text-gray-900 mb-2">Créez votre compte</h2>
            <p class="text-gray-600 mb-6">Rejoignez notre communauté de voyageurs</p>
        </div>

        <!-- Role Selection Buttons -->
        <div class="grid grid-cols-2 gap-4 mb-8">
            <button class="role-btn flex flex-col items-center justify-center p-6 rounded-xl border-2 border-gray-200 hover:border-rose-400 transition-all bg-white bg-opacity-50 group" data-role="proprietaire">
                <i data-feather="home" class="w-8 h-8 text-rose-500 mb-2 group-hover:scale-110 transition-transform"></i>
                <span class="text-lg font-medium text-gray-700">Propriétaire</span>
            </button>
            <button class="role-btn flex flex-col items-center justify-center p-6 rounded-xl border-2 border-gray-200 hover:border-rose-400 transition-all bg-white bg-opacity-50 group" data-role="voyageur">
                <i data-feather="compass" class="w-8 h-8 text-rose-500 mb-2 group-hover:scale-110 transition-transform"></i>
                <span class="text-lg font-medium text-gray-700">Voyageur</span>
            </button>
        </div>

        <!-- Social Buttons -->
        <div class="grid grid-cols-2 gap-4">
            <button class="w-full flex items-center justify-center px-4 py-2 border border-gray-300 rounded-lg text-gray-700 bg-white hover:bg-gray-50 transition-colors">
                <img src="/api/placeholder/20/20" alt="Google" class="w-5 h-5 mr-2">
                Google
            </button>
            <button class="w-full flex items-center justify-center px-4 py-2 border border-gray-300 rounded-lg text-gray-700 bg-white hover:bg-gray-50 transition-colors">
                <img src="/api/placeholder/20/20" alt="Facebook" class="w-5 h-5 mr-2">
                Facebook
            </button>
        </div>

        <div class="relative">
            <div class="absolute inset-0 flex items-center">
                <div class="w-full border-t border-gray-300"></div>
            </div>
            <div class="relative flex justify-center text-sm">
                <span class="px-2 bg-white text-gray-500 glass-effect">Ou inscrivez-vous avec</span>
            </div>
        </div>

        <!-- Registration Form -->
        <form class="space-y-6">
            <div class="grid grid-cols-2 gap-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Prénom</label>
                    <input type="text" required class="w-full px-4 py-2 rounded-lg bg-white bg-opacity-50 border border-gray-300 focus:ring-2 focus:ring-rose-400 focus:border-transparent outline-none">
                </div>
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Nom</label>
                    <input type="text" required class="w-full px-4 py-2 rounded-lg bg-white bg-opacity-50 border border-gray-300 focus:ring-2 focus:ring-rose-400 focus:border-transparent outline-none">
                </div>
            </div>

            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                <input type="email" required class="w-full px-4 py-2 rounded-lg bg-white bg-opacity-50 border border-gray-300 focus:ring-2 focus:ring-rose-400 focus:border-transparent outline-none">
            </div>

            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Numéro de téléphone</label>
                <input type="tel" required class="w-full px-4 py-2 rounded-lg bg-white bg-opacity-50 border border-gray-300 focus:ring-2 focus:ring-rose-400 focus:border-transparent outline-none">
            </div>

            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Mot de passe</label>
                <input type="password" required class="w-full px-4 py-2 rounded-lg bg-white bg-opacity-50 border border-gray-300 focus:ring-2 focus:ring-rose-400 focus:border-transparent outline-none">
            </div>

            <button type="submit" class="w-full py-3 px-4 bg-gradient-to-r from-rose-500 to-orange-500 text-white rounded-lg hover:opacity-90 transition-opacity transform hover:-translate-y-0.5 duration-200 font-medium">
                Créer mon compte
            </button>
        </form>

        <p class="text-center text-sm text-gray-600">
            Déjà membre ? 
            <a href="#" class="font-medium text-rose-500 hover:text-rose-600">Connectez-vous</a>
        </p>
    </div>

    <script>
        feather.replace();

        // Add active state to role buttons
        document.querySelectorAll('.role-btn').forEach(button => {
            button.addEventListener('click', function() {
                // Remove active state from all buttons
                document.querySelectorAll('.role-btn').forEach(btn => {
                    btn.classList.remove('border-rose-400', 'bg-rose-50');
                });
                // Add active state to clicked button
                this.classList.add('border-rose-400', 'bg-rose-50');
                // You can store the selected role
                const selectedRole = this.dataset.role;
            });
        });
    </script>
</body>
</html>